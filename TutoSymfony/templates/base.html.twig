<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>">
       <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
        {% block stylesheets %}
        {% endblock %}

        {% block javascripts %}
            {% block importmap %}{{ importmap('app') }}{% endblock %}
        {% endblock %}
    </head>

    <body>

    <nav class="navbar bg-primary navbar-expand-lg" data-bs-theme ="dark">
        <div class="container-fluid">
        <a class="navbar-brand d-inline" href="{{ url('home') }}">Mon site </a>   <!-- "home" est le nom qu'on a donné à notre route dans homecontroller -->
        <ul class="navbar-nav">
            <li class="nav-item d-inline">
                <a class="nav-link {{ app.current_route == 'home' ? 'active' : '' }}" href="{{ url('home') }}">Accueil</a>     <!--  Pour rendre actif le menu "home" quand on est sur la page home, on applique la formule suivante        {# {{ app.current_route == 'home' ? 'active' : '' }}    #}              -->
            </li>

            <li class="nav-item d-inline">
                <a class="nav-link {{ app.current_route starts with 'recipe.' ? 'active' : '' }}" href="{{ url('recipe.index') }}">Recettes</a>       <!--  Pour rendre actif le menu "Recettes" quand on est sur la page Recettes, on applique la formule suivante: {# {{ app.current_route starts with 'recipe.' ? 'active' : '' }}    #}  : qui signifie "si ça demarre par recipe., je veux que tu mettes active"               -->
            </li>

        </ul>
    </div>
    </nav>
        <div class="container my-4">
            {% block body %} {% endblock %}
        </div>

        
    </body>
</html>
